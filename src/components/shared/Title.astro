---

interface  Props {
    capitalize?: boolean;
}
const {capitalize =false} = Astro.props

---
<h1 
class:list={["text-5xl font-bold",
    { "capitalize": capitalize }
]}>
    <slot/>
</h1>

<div class="border-b-2 border-blue-500 mt-2"></div>
